# ── Core data stack ──────────────────────────────────────────────────────────
pandas>=2.0
pyarrow>=14.0
numpy>=1.24
scipy>=1.11

# ── Machine learning ──────────────────────────────────────────────────────────
scikit-learn>=1.3
joblib>=1.3

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.30
plotly>=5.18

# ── HTTP + article fetching ───────────────────────────────────────────────────
requests>=2.31
beautifulsoup4>=4.12
trafilatura>=1.6          # Best-in-class article text extraction (free)

# ── RAG: embeddings + vector search ──────────────────────────────────────────
sentence-transformers>=2.2  # all-MiniLM-L6-v2, free, runs on CPU
faiss-cpu>=1.7.4            # Local vector similarity search (no GPU needed)

# ── PDF export ────────────────────────────────────────────────────────────────
fpdf2>=2.7

# ── Optional: local LLM (Ollama must be installed separately) ─────────────────
# Ollama is NOT a Python package — install via: https://ollama.ai
# Then pull a model:  ollama pull mistral
# The dashboard auto-detects Ollama at localhost:11434 and uses it if available.
# No configuration needed — it degrades gracefully to template summaries if absent.
